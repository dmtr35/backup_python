# 📦 Python Backup Script

Простой скрипт для создания резервных копий папки и автоматического удаления старых архивов, если размер папки с бэкапами превышает лимит.

## 🚀 Возможности

- Архивирует указанную папку в `.tar.gz`
- Хранит бэкапы в отдельной директории
- Удаляет **самый старый архив**, если превышен размер (в MB)
- Автоматически создает директорию для бэкапов, если её нет

## 📌 Использование

```bash
python3 backup.py <source_dir> <backup_dir> [max_size_mb]
